// RUTA: libs/core/domain/codowhatsapp/src/lib/types/whatsapp-webhook.types.ts
// TODO: [LIA Legacy - Implementar WhatsAppWebhookTypes]
// Propósito: Definir TODAS las interfaces y tipos para los payloads de los webhooks entrantes de WhatsApp (mensajes y gestión).
// Debe ser exhaustivo y alineado con la documentación de Meta.
// Relacionado con Casos de Uso: Procesamiento de webhooks.
// export interface TWhatsAppWebhookEntry { id: string; changes: TWebhookChange[]; }
// export interface TWebhookChange { field: string; value: TWebhookValue; }
// export interface TWebhookValue { messaging_product: string; metadata: TWebhookMetadata; /* ... y los campos específicos del evento */ }
// (y así para MessagesValue, StatusValue, AccountUpdateValue, TemplateStatusUpdateValue, etc.)
