import { infrawhatsappcloudapi } from './infrawhatsappcloudapi';

describe('infrawhatsappcloudapi', () => {
  it('should work', () => {
    expect(infrawhatsappcloudapi()).toEqual('infrawhatsappcloudapi');
  });
});
