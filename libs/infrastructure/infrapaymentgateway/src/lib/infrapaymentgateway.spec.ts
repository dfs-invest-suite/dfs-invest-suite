import { infrapaymentgateway } from './infrapaymentgateway';

describe('infrapaymentgateway', () => {
  it('should work', () => {
    expect(infrapaymentgateway()).toEqual('infrapaymentgateway');
  });
});
