import { infrastorageservice } from './infrastorageservice';

describe('infrastorageservice', () => {
  it('should work', () => {
    expect(infrastorageservice()).toEqual('infrastorageservice');
  });
});
