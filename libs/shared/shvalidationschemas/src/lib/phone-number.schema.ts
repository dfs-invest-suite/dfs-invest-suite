// RUTA: libs/shared/shvalidationschemas/src/lib/phone-number.schema.ts
// TODO: [LIA Legacy - Implementar PhoneNumberSchema]
// Propósito: Un schema Zod base para números de teléfono, posiblemente con validación
// de formato internacional o regional (ej. para Brasil).
// Relacionado con Casos de Uso: Validación de leads, usuarios, datos de contacto.
// import { z } from './zod.instance';
// // Ejemplo simple, se puede hacer más robusto
// export const PhoneNumberSchema = z.string().trim().min(8).regex(/^\+?[1-9]\d{1,14}$/, 'Invalid phone number format');
